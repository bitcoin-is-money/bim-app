/**
 * Placeholder value used in development. Production and testnet builds
 * swap this file via `fileReplacements` in angular.json for a generated
 * `build-version.prod.ts` that holds the real epoch-ms build timestamp.
 *
 * A value of 0 means "dev mode" and instructs `PwaUpdateService` to never
 * report an available update.
 */
export const BUILD_TIMESTAMP = 0;

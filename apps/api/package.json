{
  "name": "@bim/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist && find public -mindepth 1 ! -name '.gitkeep' -delete 2>/dev/null || true",
    "clean:all": "npm run clean && rm -rf node_modules",
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && esbuild src/main.ts --bundle --platform=node --format=esm --outfile=dist/main.js --packages=external",
    "dev": "tsc --watch --noEmit",
    "start": "node dist/main.js",
    "start:dev": "tsx watch src/main.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run --config vitest.config.integration.ts",
    "test:all": "npm run test && npm run test:integration",
    "copy:front": "cp -r ../front/dist/frontend/browser/browser/* ./public/",
    "db:generate": "drizzle-kit generate --config drizzle.config.ts",
    "db:migrate": "drizzle-kit migrate --config drizzle.config.ts",
    "db:push": "drizzle-kit push --config drizzle.config.ts",
    "db:studio": "drizzle-kit studio --config drizzle.config.ts"
  },
  "dependencies": {
    "@atomiqlabs/chain-starknet": "^7.0.25",
    "@atomiqlabs/sdk": "^7.0.10",
    "@atomiqlabs/storage-sqlite": "^4.0.0",
    "@bim/domain": "*",
    "@bim/lib": "*",
    "@hono/node-server": "^1.13.0",
    "dotenv": "^17.2.3",
    "dotenv-expand": "^12.0.3",
    "drizzle-orm": "^0.38.0",
    "hono": "^4.6.0",
    "light-bolt11-decoder": "^3.2.0",
    "pg": "^8.13.0",
    "starknet": "^9.3.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@bim/test-toolkit": "*",
    "@testcontainers/postgresql": "^11.11.0",
    "@types/node": "^25.0.9",
    "@types/pg": "^8.11.6",
    "drizzle-kit": "^0.30.0",
    "testcontainers": "^11.11.0"
  }
}

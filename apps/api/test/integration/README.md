
The idea with integration tests was to run them locally.

But... integration tests using devnet container will be probably removed because :
 - they are slower than testnet
 - they use mocks, testnet do not
 - they need a complex setup



